import { webinars_json } from '../webinars.js'

const initialState = {
  webinars: webinars_json
}

// console.log(initialState);

export default function webinars(state = initialState, action) {
  // switch (action.type) {
  //   case types.SET_WEBINAR_URL:
  //     return state
  //
  //
  //   default:
  //     return state
  //
  // }
  return state
}